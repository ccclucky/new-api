# new-api

AI API 网关：把数十家上游模型聚合到统一 API 之后，带用户、令牌、分组与计费体系。

## Language

### 智能路由 (Smart Routing)

**智能路由**:
网关按任务复杂度为请求自动挑选上游模型，客户端不必指定具体模型。
_Avoid_: 自动分组（那是分组选择概念，两回事）

**虚拟模型名**:
客户端可见的合成模型名（默认 `auto`，可配），代表「由网关代为选模」，不对应任何真实上游模型。
_Avoid_: 别名、映射模型

**候选池**:
一次请求中路由器可选择的模型集合 = 该令牌分组下全部可用模型 ∩ 令牌允许模型。
_Avoid_: 模型列表、候选集

**决策模型**:
外部分类器（Jev / TypeSafe AI System One），回答「该用哪个模型」的封闭问题，不转发 LLM 请求。
_Avoid_: 路由网关

**兜底模型**:
决策模型失败或其答案不在候选池时使用的缺省模型；请求不因路由器不可用而失败。
_Avoid_: 降级模型

**路由决策**:
一次路由的完整结果（所选模型、置信分数、耗时、失败原因），随用量日志记录。

**模型粘性**:
多轮对话锁定首轮所选模型的机制。本功能 v1 明确不做，每轮独立路由。
_Avoid_: 渠道亲和（那是既有渠道级机制，不同层）
