# 03 可见性：/v1/models、定价页、日志

Status: ready-for-agent
Blocked by: 01

## 内容

- `/v1/models` 列表与定价页在功能开启时展示虚拟名条目（标注「智能路由」）；不编造价格，路由后按真实模型计费。
- 用量日志记路由决策：所选模型、来源（jev/兜底原因）、耗时。复用现有日志扩展字段机制，不加新表。

## 验收

开启后 `GET /v1/models` 含 `auto`；关闭后不含。完成一次 auto 请求后日志可见决策行。
